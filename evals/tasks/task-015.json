{
  "id": "task-015",
  "prompt": "Create recover.py with an answer() function that returns 42. Before finishing, execute a direct check of answer(), notice and correct any incorrect result, and leave the final implementation clean and importable.",
  "verification_command": "test \"$(python3 -c 'from recover import answer; print(answer())')\" = 42"
}
