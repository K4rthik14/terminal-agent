{
  "id": "task-002",
  "prompt": "Create a file named settings.txt containing the line mode=old, then modify it so the line is exactly mode=new",
  "verification_command": "test -f settings.txt && grep -qx 'mode=new' settings.txt && ! grep -q 'mode=old' settings.txt"
}
